import './bootstrap';

// NOTE: Do NOT import or start Alpine here. Livewire 3 bundles and starts its own
// Alpine — adding a second instance throws "Alpine has already been initialized"
// and silently breaks every wire:click on the page.
